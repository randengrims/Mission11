export interface Project {
  bookId: number;
  title: string;
  author: string;
  publisher: string;
  isbn: string;
  classification: number;
  category: string;
  pageCount: number;
  price: number;
}
